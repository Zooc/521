
def gitBranchName() {
    def cmd = "git symbolic-ref --short -q HEAD"
    return cmd.execute().text.trim()
}

def brName = gitBranchName()
if(brName=="master"){
    include ':app'
}else{
    include ':app', ':javazoo', ':androidzoo'
}
